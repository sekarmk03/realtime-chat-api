const userSvc = require('./user');
const authSvc = require('./auth');
const chatSvc = require('./chat');
const messageSvc = require('./message');

module.exports = {
    userSvc,
    authSvc,
    chatSvc,
    messageSvc,
};